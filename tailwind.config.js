// ============================================================================
// TailwindCSS 設定ファイル (学習用コメント付き)
// ============================================================================
// TailwindCSS v4の設定ファイル
// DioxusプロジェクトでTailwindCSSを使用する際の設定例
//
// 学習ポイント:
// - TailwindCSS v4の新しい設定方式
// - Rustファイル（.rs）のスキャン設定
// - コンテンツ検出の仕組み
// - Dioxusとの統合方法

/** @type {import('tailwindcss').Config} */
module.exports = {
  // ============================================================================
  // モード設定
  // ============================================================================
  // "all": 全てのTailwindCSSユーティリティを生成
  // 学習ポイント: 開発時は"all"、本番時は使用されるクラスのみ生成も可能
  mode: "all",

  // ============================================================================
  // コンテンツスキャン設定
  // ============================================================================
  // TailwindCSSが使用されているファイルを指定
  // 学習ポイント: 
  // - Rustファイル（.rs）も対象に含める（Dioxus特有）
  // - HTML、CSSも対象（標準的な設定）
  // - dist/ディレクトリもスキャン（ビルド成果物）
  content: [
    "./src/**/*.{rs,html,css}",  // srcディレクトリ内のRust、HTML、CSSファイル
    "./dist/**/*.html"           // distディレクトリ内のHTMLファイル（ビルド後）
  ],

  // ============================================================================
  // テーマ設定
  // ============================================================================
  // TailwindCSSのデフォルトテーマをカスタマイズ
  // 学習ポイント: extendを使用してデフォルト値を保持しつつ拡張
  theme: {
    extend: {
      // ここでカスタムカラー、フォント、スペーシング等を追加可能
      // 例：
      // colors: {
      //   'game-blue': '#1e40af',
      //   'game-red': '#ef4444',
      // },
      // fontFamily: {
      //   'game': ['Comic Sans MS', 'cursive'],
      // }
    },
  },

  // ============================================================================
  // プラグイン設定
  // ============================================================================
  // TailwindCSSプラグインを追加
  // 学習ポイント: 
  // - 現在は空配列（プラグインなし）
  // - 必要に応じてフォーム、タイポグラフィ等のプラグインを追加可能
  plugins: [
    // 例：
    // require('@tailwindcss/forms'),
    // require('@tailwindcss/typography'),
  ],
};
